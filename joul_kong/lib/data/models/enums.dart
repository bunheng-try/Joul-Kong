enum BikeStatus { available, reserved, inUse, maintenance }

enum SlotStatus { occupied, empty}

enum BookingStatus { active, cancelled, expired }

enum PassStatus { active, expired }

enum PaymentType { ticket, pass }

enum PaymentMethod { card, wallet }

enum PaymentStatus { success, failed }

enum TicketStatus { active, used, expired }
